---
  el:
    hosts:
      centos7:
        ansible_connection: docker
  deb:
    hosts:
      ubuntu:
        ansible_connection: docker
  local:
    hosts:
      ansibleserver:
        ansible_connection: ssh

  fed:
    hosts:
      fedora:
        ansible_connection: docker
